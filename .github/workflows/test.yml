name: Unit Test

on:
  push:

jobs:
  test:
    name: Test (${{ matrix.engine.name }} ${{ matrix.engine.version }})
    strategy:
      fail-fast: false
      matrix:
        engine:
          - name: ruby
            version: '3.4'
          - name: ruby
            version: '3.3'
          - name: ruby
            version: '3.2'
          - name: ruby
            version: '3.1'
          - name: ruby
            version: '3.0'
          - name: ruby
            version: '2.7'
          - name: ruby
            version: '2.6'
          - name: ruby
            version: '2.5'
          - name: jruby
            version: '9.4'
          - name: jruby
            version: '9.3'
          - name: jruby
            version: '9.2'
    uses: ./.github/workflows/test-callable.yml
    with:
      engine_name: ${{ matrix.engine.name }}
      engine_version: ${{ matrix.engine.version }}

