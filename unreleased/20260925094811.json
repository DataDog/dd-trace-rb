{
  "type": "Fixed",
  "product": "Tracing",
  "pull_request": "https://github.com/DataDog/dd-trace-rb/pull/6384",
  "message": "Fix the OpenTelemetry Thread Context leaking the previous thread's trace and span identifiers onto a new thread that reused a recycled native thread, which could happen when a thread running traced code was terminated with `Thread#kill` on Ruby < 3.3."
}
