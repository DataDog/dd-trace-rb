{
  "type": "Fixed",
  "prefix": "Tracing",
  "pull_request": "https://github.com/DataDog/dd-trace-rb/pull/6020",
  "message": "Fix `pg` instrumentation which raised `ArgumentError` when calling `exec_params`, `exec_prepared` without a `params` argument, or their `async_`/`sync_` variants."
}
